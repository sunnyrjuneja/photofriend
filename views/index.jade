extends layout

block content
  .container-fluid
    .row-fluid
      .span2
          legend Photo Friends
          label Upload a photo to share with the world.
          .controls.controls-row
            input(type="button", id="fpicker", value="Upload") 
      #images.span10
        each photo in photos
          .img-crop
            img(src=photo.url)
